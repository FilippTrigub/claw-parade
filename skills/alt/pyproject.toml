[project]
name = "clawvlm"
version = "0.1.0"
description = "Auto-describe and caption images using a local vision-language model"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "pillow",
    "einops",
    "torchvision",
]
