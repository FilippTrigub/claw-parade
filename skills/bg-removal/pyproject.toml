[project]
name = "clawbg"
version = "0.1.0"
description = "Batch background removal for images using rembg"
requires-python = ">=3.10"
dependencies = [
    "rembg[gpu]>=2.0",
    "pillow",
    "numpy",
]
