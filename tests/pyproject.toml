[project]
name = "claw-tests"
version = "0.1.0"
description = "Integration tests for claw-parade skills"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pytest>=8.0",
    "pillow",
    "numpy",
]
