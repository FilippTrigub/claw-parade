[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show short test summary for all outcomes
addopts = -v --tb=short --no-header

# Custom markers
markers =
    gpu: marks tests that require a CUDA GPU (auto-skipped via requires_gpu fixture)
